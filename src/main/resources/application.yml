management:
  info:
    java:
      enabled: true
    os:
      enabled: true
  server:
    port: 8080
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"